<h2>Users</h2>

<table>
  <thead>
    <tr>
      <th>image</th>
      <th>name</th>
    </tr>
  </thead>

  <tbody>
    <% @users.each do |user| %>
      <%= user.profile_image.attached? %>
      <%= user.name %>
    <% end %>
  </tbody>
</table>

